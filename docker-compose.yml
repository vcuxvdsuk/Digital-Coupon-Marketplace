services:
    app:
        build: .
        ports:
            - "8080:8080"
        depends_on:
            - db
        environment:
            - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/couponmarketplace
            - SPRING_DATASOURCE_USERNAME=postgres
            - SPRING_DATASOURCE_PASSWORD=password
            - RESELLER_API_TOKEN=static-token-123
    db:
        image: postgres:15
        environment:
            - POSTGRES_DB=couponmarketplace
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=password
        ports:
            - "5432:5432"
